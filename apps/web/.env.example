# Path to SQLite database file (relative to apps/web)
DATABASE_URL=./data/dynamically.db

# Directory to store uploaded files (relative to apps/web)
UPLOAD_DIR=./uploads

# Public URL where this instance is reachable (used for upload URLs)
PUBLIC_URL=http://localhost:3030

# Node env
NODE_ENV=development
